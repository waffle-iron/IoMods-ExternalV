var mdns = require('mdns')

var ad = mdns.createAdvertisement(mdns.tcp('http'),3000,{name : 'Node Server'}

ad.start(5
ad : true,
)

server.start[server.properties] {
var mdns = ('https')online,3000,
server.properties = mdns('script')
var server.properties = mineos-node || Server = on,







}
